APP_ENV=dev
APP_SECRET=dev_secret_change_in_prod
APP_DEBUG=1

# Stockage des fichiers du file manager (adaptateur Flysystem Local) :
# demo/var/storage/ (relatif au dossier demo = kernel.project_dir)

# Optionnel : MinIO (S3) pour tester le file manager avec un filesystem S3.
# IMPORTANT : Décommenter et adapter ces lignes si vous voulez utiliser le filesystem S3.
# Sinon, le sélecteur "S3 / MinIO" dans le filemanager ne fonctionnera pas.
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=filemanager-demo
MINIO_REGION=us-east-1

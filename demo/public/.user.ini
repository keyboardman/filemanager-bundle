; Limites PHP pour la démo (uploads volumineux)
; Pris en compte par PHP-FPM / Apache. Pour symfony serve, voir .symfony.local.ini à la racine.
; Redémarrer le serveur après modification.

memory_limit = 256M
upload_max_filesize = 100M
post_max_size = 100M

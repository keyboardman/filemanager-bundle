<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Manager</title>
    <link rel="stylesheet" href="{{ asset('bundles/keyboardmanfilemanager/build/manager.css') }}">
</head>
<body>
    <div class="layout"
         data-api-base="{{ api_base|e('html_attr') }}"
         data-initial-path="{{ initial_path|e('html_attr') }}"
         data-initial-filter-type="{{ initial_filter_type|e('html_attr') }}"
         data-initial-filter-search="{{ initial_filter_search|e('html_attr') }}"
         data-initial-sort="{{ initial_sort|e('html_attr') }}"
         data-initial-filesystem="{{ initial_filesystem|e('html_attr') }}"
         data-available-filesystems="{{ available_filesystems|json_encode|e('html_attr') }}"
         data-picker="{{ picker ? '1' : '0' }}"
         data-channel="{{ channel|e('html_attr') }}">
    </div>
    <script src="{{ asset('bundles/keyboardmanfilemanager/build/manager.js') }}"></script>
</body>
</html>
